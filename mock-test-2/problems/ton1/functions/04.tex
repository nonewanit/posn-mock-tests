% topic: function
\question กำหนด $f: \mathbb{R} \to \mathbb{R} $ โดย $f(x)=a^{x}$ เมื่อ $a$ เป็นจำนวนจริงซึ่ง $a>0$ และ $a \neq 1$ ข้อใดกล่าวไม่ถูกต้อง
\newline
\choicesgrid{$f$ เป็นฟังก์ชันหนึ่งต่อหนึ่ง (one-to-one)}{จุดตัดแกน $y$ ของกราฟ $f$ คือ $(0, 1)$}{$f$ ไม่มีจุดตัดแกน $x$}{$f$ เป็นฟังก์ชันเพิ่ม (เมื่อ $x$ เพิ่มขึ้น $y$ จะเพิ่มขึ้นเสมอ)}
% answer: 4